[package]
name = "edge-connector"
version = "0.1.0"
edition = "2018"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "json"] }
sysinfo = "0.20"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
